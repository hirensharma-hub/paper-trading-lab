# Examples

`sample-format.csv` is a **FORMAT EXAMPLE ONLY — NOT MARKET DATA**. It is intentionally too small for an experiment and exists only to show the accepted CSV columns and timestamp formats.

Use `historical-manifest.example.json` and `first-historical.example.json` as templates. Replace every placeholder with user-supplied, permitted historical data metadata before running a historical experiment.
