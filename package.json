{
  "name": "paper-trading-lab",
  "private": true,
  "version": "0.1.0",
  "description": "Deterministic, paper-only quantitative trading research foundation.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test tests/*.test.mjs"
  }
}
